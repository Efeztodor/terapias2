# Backend (servidor Node en server/index.js)
# Railway inyecta DATABASE_URL al añadir el plugin PostgreSQL.
DATABASE_URL=

# Frontend: URL de la API (opcional).
# En desarrollo con proxy (npm run dev + npm run dev:server) déjalo vacío.
# En producción en Railway déjalo vacío si frontend y API están en el mismo dominio.
VITE_API_URL=
